// this type is for the camera angle for the scene
export type TPosition = [x: number, y: number, z: number];

// this type is for determining which axis we are adjusting;
export type TAxis = 'x' | 'y' | 'z';